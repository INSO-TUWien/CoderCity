import { BranchSimpleNamePipe } from './branch-simple-name.pipe';

describe('BranchSimpleNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BranchSimpleNamePipe();
    expect(pipe).toBeTruthy();
  });
});
